part of '../pages.dart';

class RekapPermintaanSection extends StatelessWidget {
  const RekapPermintaanSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
