part of '../pages.dart';

class DashboardAdminSection extends StatelessWidget {
  const DashboardAdminSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child : Text('Dashboard - Balai Besar Wilayah II Ciputat '),
    );
  }
}
