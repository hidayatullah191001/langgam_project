part of '../pages.dart';

class PelayananMasukSection extends StatelessWidget {
  const PelayananMasukSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
